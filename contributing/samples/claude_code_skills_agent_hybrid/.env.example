# Vertex AI / model configuration
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_GENAI_USE_VERTEXAI=TRUE
GOOGLE_API_KEY=

# Optional: restrict file operations to this directory
# ADK_HYBRID_SKILLS_PROJECT_ROOT=/path/to/your/repo

# No code executor configuration needed.
# The hybrid sample imports trusted Python helper code as modules and
# rewrites bash behavior as native Python tools.
